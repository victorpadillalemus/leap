<div class="confirmation-page">
  <div class="qr-card">
    <%= image_tag "https://peopledotcom.files.wordpress.com/2018/06/shutterstock_editorial_9709210a_huge.jpg", class: "avatar-large", alt: "avatar-large" %>
      <div>Your Guide will be:</div>
      <h4>Vince</h4>
      <div class="confirmation-number">Confirmation # <%= rand(1..9999999999) %></div>
        <div><i class="fas fa-print"></i> Save this QR code!</div>
      <div class="qrcode-container">
        <%= image_tag("qr-code.png") %>
      </div>
  </div>
    <div class="map-container">
     <div><i class="fas fa-location-arrow"></i> Find us here holding a leap sign!</div>
     <div id="map"
     style="width: 100%;
     height: 200px;"data-markers="<%= @markers.to_json %>"
     data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
     ></div>
   </div>
 </div>
</div>

 <div class="footer">
   <%= link_to 'My Bookings', bookings_path(@bookings), class: 'primary-button', method: :get %>
 </div>
